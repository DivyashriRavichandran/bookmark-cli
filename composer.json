{
    "name": "divyashriravichandran/bookmark-cli",
    "description": "A PHP command-line tool for saving and indexing webpages. Features high-speed lookups via a Redis RAM cache, falling back to real-time HTML scraping, persistent JSON storage, and Elasticsearch indexing on a cache miss.",
    "type": "library",
    "license": "MIT",
    "bin": [
        "bookmark"
    ],
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "require": {
        "elasticsearch/elasticsearch": "^9.4",
        "predis/predis": "^3.5"
    },
    "config": {
        "allow-plugins": {
            "php-http/discovery": true
        }
    }
}